---
slug: installation
---

# 安装 Tai Link

在目标机器上下载并安装 Tai Link。

## 下载

前往[下载页面](/download/tai)获取对应平台的 Tai Link。

| 平台 | 文件 |
|------|------|
| macOS (Apple Silicon) | `tai-darwin-arm64` |
| macOS (Intel) | `tai-darwin-amd64` |
| Linux (x86-64) | `tai-linux-amd64` |
| Linux (ARM64) | `tai-linux-arm64` |
| Windows (x86-64) | `tai-windows-amd64.exe` |

**macOS / Linux:**

```bash
chmod +x tai-*
sudo mv tai-* /usr/local/bin/tai
```

**Windows:** 打开 PowerShell，将 `.exe` 移动到系统 PATH 目录：

```powershell
Move-Item tai-windows-amd64.exe C:\Windows\tai.exe
```

## 验证

**macOS / Linux:**

```bash
tai version --all
```

```
Version:    1.8.6
Git Commit: 991a213
Build Time: 2026-08-21T06:25:18Z
Go Version: go1.25.12
OS/Arch:    darwin/arm64
```

**Windows (PowerShell):**

```powershell
tai version --all
```

```
Version:    1.8.6
Git Commit: 991a213
Build Time: 2026-08-21T06:25:20Z
Go Version: go1.25.13
OS/Arch:    windows/amd64
```

看到版本信息即表示安装成功。

## 下一步

- [连接第一台设备](/docs/zh-cn/tai-link/connect-first-device) — 创建 Server Key、编写配置文件并启动 Tai Link
