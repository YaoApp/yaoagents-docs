---
slug: deepseek-harness
---

# DeepSeek Harness

![DeepSeek Harness](https://assets.yaoagents.com/en/experts/04-expert-deepseek-harness-en-$__THEME.png)

基于 DeepSeek Harness 的通用智能体。编程、研究、数据分析、写作、自动化——在持久化沙箱中完成一切，终端、文件系统、浏览器和 VNC 桌面全部就绪。

## 擅长什么

- **编程** — 编写、运行、调试和重构任何语言的程序；搭建和维护复杂项目
- **研究** — 抓取网页、综合多源信息、生成结构化报告
- **数据分析** — 处理 CSV、JSON、Excel 文件；运行分析；生成图表和看板
- **写作** — 撰写报告、方案、邮件、Markdown 或 HTML 文档
- **自动化** — 构建脚本、CLI 工具和流水线，将重复工作自动化
- **实时预览** — 在沙箱中运行应用和工具，通过 VNC 实时查看结果

## 工作原理

DeepSeek Harness 运行在持久化的 Linux 沙箱中，配备完整桌面环境。拥有终端、浏览器、文件系统和网络访问——相当于一台完整的工作机器。

它使用 DeepSeek V4 Pro 或 V4 Flash 作为推理引擎。你描述需求，它规划方案、执行工作、观察结果并反复迭代，直到满意为止。

沙箱生命周期为 `longrunning`——空闲时暂停，回来时自动恢复。代码、文件和项目状态跨对话永久保存。

## 提示词技巧

**描述目标，不要描述步骤。** "写一个用 SQLite 管理待办事项的 REST API" 就够了。不需要指定框架或文件结构。

**调试时提供上下文。** 粘贴错误信息、相关代码，或两者都给。上下文越多，修复越快。

**自然地迭代。** 第一次结果出来后，继续提要求："加上认证"、"提升性能"、"写测试"。沙箱保留状态，每次请求都在上一次基础上继续。

**示例：**

> 写一个 Python 脚本，读取客户订单 CSV 文件，生成按地区汇总的报表。

> 调试这个 Node.js 服务——POST 请求会崩溃。这是报错信息：[粘贴]

> 做一个网页爬虫，从这三个 URL 抓取产品价格，保存到 JSON 文件。

> 搭建一个带 JWT 认证的 Flask API，连接 SQLite，添加博客 CRUD 接口。

## 能力一览

| 能力 | 说明 |
|------|------|
| 模型 | DeepSeek V4 Pro / V4 Flash（可配置） |
| 语言 | Python、Node.js、Bash，以及任何可安装的语言 |
| 网络访问 | 浏览网页、抓取数据、调用 API |
| 文件读写 | 支持任何文件格式 |
| 预览 | VNC 实时桌面——即时查看应用运行效果 |
| 沙箱 | 持久化 Linux 桌面，完整终端和浏览器 |
| 生命周期 | 长期运行——空闲暂停，返回恢复 |
| 工作区 | 永久存储——代码和文件跨对话保留 |

## 接下来

- [Claude Code](/docs/zh-cn/built-in-agents/claude-code) — 同样的沙箱能力，由 Claude 驱动
- [Applet Workshop](/docs/zh-cn/built-in-agents/applet) — 快速构建可视化小工具
- [AI 专家](/docs/zh-cn/getting-started/ai-experts-intro) — 查看所有内置专家
